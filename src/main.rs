/// Entry point for `lazypackage`.
///
/// # Status
///
/// Not yet implemented. The TUI and backend adapters are still under
/// development. This file will be wired up once the following crates are
/// ready:
///
/// - `lazypackage-tui`      — ratatui rendering & event loop
/// - `lazypackage-backends` — apt / dnf / pacman / zypper adapters
#[tokio::main]
async fn main() {
    todo!("lazypackage entry point — TUI and backends not yet implemented");
}
